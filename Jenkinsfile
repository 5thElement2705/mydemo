pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Add your build steps here
                sh 'your-build-command'
            }
        }

        stage('Test') {
            steps {
                // Add your test steps here
                sh 'your-test-command'
            }
        }

        stage('Deploy') {
            steps {
